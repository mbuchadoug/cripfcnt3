<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>CRIPFCnt SCOI Audit Result</title>
  <link href="https://cdn.jsdelivr.net/npm/tailwindcss@2.2.19/dist/tailwind.min.css" rel="stylesheet">
</head>
<body class="bg-gray-100 text-gray-900 leading-relaxed antialiased">

  <div class="max-w-4xl mx-auto my-10 bg-white p-10 shadow-lg rounded-2xl">
    <h1 class="text-3xl font-bold mb-6 text-center text-blue-700">
      🧩 CRIPFCnt SCOI Audit — {{organization}} ({{year}})
    </h1>

    <section class="mb-6">
      <h2 class="text-xl font-semibold mb-2">1️⃣ Visibility (Capacity / Denominator)</h2>
      <p><strong>Score:</strong> {{visibility}} / 10</p>
      <p class="mt-2">{{visibilityNotes}}</p>
    </section>

    <section class="mb-6">
      <h2 class="text-xl font-semibold mb-2">2️⃣ Contribution (Structural Recalibration / Numerator)</h2>
      <p><strong>Score:</strong> {{contribution}} / 10</p>
      <p class="mt-2">{{contributionNotes}}</p>
    </section>

    <section class="mb-6">
      <h2 class="text-xl font-semibold mb-2">3️⃣ SCOI Calculation</h2>
      <p>Raw SCOI = {{rawSCOI}}</p>
      <p class="mt-2 italic">{{rawInterpretation}}</p>
    </section>

    <section class="mb-6">
      <h2 class="text-xl font-semibold mb-2">4️⃣ Global Environment Adjustment</h2>
      <p><strong>ERF:</strong> {{ERF}}</p>
      <p class="mt-2">{{ERFNotes}}</p>
    </section>

    <section class="mb-6">
      <h2 class="text-xl font-semibold mb-2">5️⃣ Adjusted SCOI Calculation</h2>
      <p class="text-lg font-bold text-green-600">
        Final Adjusted SCOI = {{adjustedSCOI}}
      </p>
    </section>

    <section class="mt-8 border-t pt-4">
      <h2 class="text-xl font-semibold mb-2">6️⃣ CRIPFCnt Commentary</h2>
      <blockquote class="italic text-gray-700 border-l-4 border-blue-500 pl-4">
        {{commentary}}
      </blockquote>
    </section>

    <footer class="mt-8 text-center text-gray-500 text-sm">
      © {{year}} CRIPFCnt System Theory — Stage II Civilization Metrics
    </footer>
  </div>

</body>
</html>
