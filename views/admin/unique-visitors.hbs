<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8" />
  <title>{{title}}</title>
  <style>
    :root {
      --bg: #f5f7fa;
      --card-bg: #ffffff;
      --accent: #0066cc;
      --accent-soft: #e1edff;
      --accent-dark: #004c99;
      --border-soft: #e1e5ec;
      --text-main: #1f2933;
      --text-muted: #6b7280;
      --nav-bg: #ffffff;
      --nav-border: #e5e7eb;
    }

    * { box-sizing: border-box; }

    body {
      margin: 0;
      padding: 0;
      font-family: system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
      background: var(--bg);
      color: var(--text-main);
    }

    /* NAVBAR */
    .navbar {
      position: fixed;
      top: 0;
      width: 100%;
      height: 60px;
      background: var(--nav-bg);
      border-bottom: 1px solid var(--nav-border);
      display: flex;
      align-items: center;
      justify-content: space-between;
      padding: 0 20px;
      z-index: 999;
    }

    .nav-left {
      font-size: 1.1rem;
      font-weight: 700;
      color: var(--accent-dark);
    }

    .nav-links {
      display: flex;
      gap: 18px;
    }

    .nav-links a {
      text-decoration: none;
      color: var(--accent-dark);
      font-weight: 600;
      padding: 6px 10px;
      border-radius: 6px;
    }

    .nav-links a:hover {
      background: var(--accent-soft);
    }

    /* PAGE LAYOUT */
    .page {
      padding: 100px 24px 24px;
      max-width: 1100px;
      margin: 0 auto;
    }

    header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 18px;
      gap: 12px;
    }

    h1 {
      margin: 0;
      font-size: 1.5rem;
      font-weight: 800;
      color: #003366;
    }

    .subtitle {
      margin-top: 4px;
      font-size: 0.95rem;
      color: var(--text-muted);
    }

    .layout-grid {
      display: grid;
      grid-template-columns: 1.2fr 2fr;
      gap: 20px;
      margin-bottom: 24px;
    }

    @media (max-width: 860px) {
      .layout-grid { grid-template-columns: 1fr; }
    }

    .card {
      background: var(--card-bg);
      border-radius: 14px;
      padding: 16px 18px;
      box-shadow: 0 8px 20px rgba(15,23,42,0.04);
      border: 1px solid var(--border-soft);
    }

    .card-header {
      display: flex;
      justify-content: space-between;
      align-items: center;
      margin-bottom: 6px;
    }

    .card-title { font-size: 0.95rem; font-weight: 700; color: var(--accent-dark); }

    .metric-value { font-size: 1.9rem; font-weight: 800; color: #111827; }

    .badge {
      padding: 4px 9px;
      border-radius: 999px;
      background: var(--accent-soft);
      color: var(--accent-dark);
      font-size: 0.75rem;
      font-weight: 600;
    }

    /* Filters */
    .filters { display: flex; flex-wrap: wrap; gap: 8px; margin-top: 8px; }
    .filters label { font-size: 0.8rem; color: var(--text-muted); display: flex; flex-direction: column; gap: 4px; }
    .filters select,
    .filters input[type="number"],
    .filters input[type="text"] {
      padding: 6px 8px;
      border-radius: 8px;
      border: 1px solid var(--border-soft);
      font-size: 0.85rem;
      min-width: 90px;
    }

    .filters button {
      padding: 7px 13px;
      border-radius: 999px;
      border: none;
      background: var(--accent);
      color: #fff;
      font-size: 0.85rem;
      cursor: pointer;
    }

    /* Table */
    .table-card {
      overflow: hidden;
      padding: 0;
    }

    table {
      border-collapse: collapse;
      width: 100%;
      font-size: 0.85rem;
      min-width: 420px;
    }

    th, td {
      padding: 10px 12px;
      border-bottom: 1px solid #e5e7eb;
      white-space: nowrap;
      text-align: left;
    }

    thead { background: #f9fafb; }

    tbody tr:nth-child(even) { background: #fafbff; }

    tbody tr:hover { background: #eef4ff; }

    .td-period { font-family: monospace; }

    .footer-note { margin-top: 14px; font-size: 0.75rem; color: var(--text-muted); }
  </style>
</head>
<body>

  <!-- NAVBAR -->
  <nav class="navbar">
    <div class="nav-left">Admin Dashboard</div>
    <div class="nav-links">
      <a href="/admin/users">Users</a>
      <a href="/admin/visitors-live">Live Logs</a>
      <a href="/admin/visits">Visits</a>
      <a href="/admin/unique-visitors" style="background:var(--accent-soft)">Unique</a>
      <a href="/admin/audit">SCOI</a>
       <a href="/auth/logout">Sign out</a>
    </div>
  </nav>

  <div class="page">
    <header>
      <div>
        <h1>Unique visitors</h1>
        <p class="subtitle">High-level view of distinct visitors across your surfaces.</p>
      </div>
    </header>

    <div class="layout-grid">
      <!-- Left Card -->
      <section class="card">
        <div class="card-header">
          <div>
            <div class="card-title">Global unique footprint</div>
            <div class="metric-value">{{totalUnique}}</div>
          </div>
          <span class="badge">● Live metric</span>
        </div>

        <form class="filters" method="get" action="/admin/unique-visitors">
          <label>
            Period
            <select name="period">
              <option value="day" {{#if isDay}}selected{{/if}}>Day</option>
              <option value="month" {{#if isMonth}}selected{{/if}}>Month</option>
              <option value="year" {{#if isYear}}selected{{/if}}>Year</option>
            </select>
          </label>

          <label>
            Lookback
            <input type="number" name="days" min="1" max="365" value="{{days}}"/>
          </label>

          <label>
            Path
            <input type="text" name="path" placeholder="/ or /audit" value="{{path}}"/>
          </label>

          <button type="submit">Apply</button>
        </form>
      </section>

      <!-- Right Card: Table -->
      <section class="card table-card">
        <div class="card-header" style="padding:16px 18px 0 18px;">
          <div class="card-title">Time-series</div>
          <span class="badge">{{period}} view</span>
        </div>

        <div class="table-wrapper">
          {{#if series.length}}
            <table>
              <thead>
                <tr>
                  <th>Bucket ({{period}})</th>
                  <th>Unique visitors</th>
                </tr>
              </thead>
              <tbody>
                {{#each series}}
                <tr>
                  <td class="td-period">{{_id}}</td>
                  <td class="td-count">{{uniqueCount}}</td>
                </tr>
                {{/each}}
              </tbody>
            </table>
          {{else}}
            <div class="empty-state">No data found.</div>
          {{/if}}
        </div>
      </section>
    </div>

    <p class="footer-note">
      Unique visitor IDs are tracked per <strong>day × visitor × path</strong> and aggregated across periods.
    </p>
  </div>

</body>
</html>
